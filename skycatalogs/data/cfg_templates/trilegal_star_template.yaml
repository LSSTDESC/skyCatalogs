area_partition:
  nside: 32
  ordering: ring
  type: healpix
data_file_type: parquet
file_template: trilegal_star_(?P<healpix>\d+).parquet
flux_file_template: trilegal_star_flux_(?P<healpix>\d+).parquet
internal_extinction: None

# Will need to be changed, depending on form of SED files and where they're
# kept
sed_file_root_env_var: SIMS_SED_LIBRARY_DIR
sed_model: file_nm
file_nm:
  units: nm
